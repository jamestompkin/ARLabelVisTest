"""Scripts that drive experiments and produce artifacts (LUTs, figures, CSVs).

Run each script with `uv run python -m scripts.<name>` from the repo root.
Each script keeps its parameters as constants at the top — edit and re-run.
"""
